# 使用指南（給非工程師）

這份文件假設你沒有寫過程式。照著做就可以。

---

## 一、這個程式是什麼

一個**網頁儀表板**，把 NVIDIA H100、H200、B200、AMD MI300X、Google TPU 等 AI 晶片
在 12 家雲端供應商的「每小時租金」整理成圖表，並追蹤價格變化。

它由兩個部分組成：

| 部分 | 做什麼 | 你需要做什麼 |
|---|---|---|
| **網頁** | 把資料畫成圖表 | 打開它就好 |
| **收集程式** | 每天去各家網站抓價格 | 設定一次，之後全自動 |

---

## 二、最快的方法：把它變成一個真正的網站

**推薦這個方法。你不用安裝任何軟體，全部在 GitHub 網站上點一點就好。**
完成後你會得到一個網址，手機電腦都能開，而且每天早上自動更新價格。

你的網址將會是： **https://b9907051.github.io/test/**

### 步驟 1：允許程式自動存檔

1. 打開 https://github.com/b9907051/test/settings/actions
2. 捲到最下面找到 **Workflow permissions**
3. 選 **Read and write permissions**（讀寫權限）
4. 按 **Save**

> 為什麼要這一步：收集程式每天抓完價格後，需要把新價格存回你的 repo。沒有這個權限它存不進去。

### 步驟 2：打開網站功能

1. 打開 https://github.com/b9907051/test/settings/pages
2. 在 **Source**（來源）下拉選單，選 **GitHub Actions**
3. 不用按儲存，選了就生效

### 步驟 3：手動跑第一次

1. 打開 https://github.com/b9907051/test/actions
2. 左邊點 **Update prices & deploy**
3. 右邊點灰色按鈕 **Run workflow**，再點綠色的 **Run workflow** 確認
4. 等 1 到 3 分鐘，畫面上會出現一列，前面的圈圈轉完變成 **綠色打勾** 就成功了

### 步驟 4：打開你的網站

等綠色打勾出現後，打開 **https://b9907051.github.io/test/**

> 第一次可能要多等 1 到 2 分鐘網址才會生效。如果出現 404，泡杯茶再重整一次。

**完成後就不用再管它了。** 每天台北時間早上 11:17 會自動抓一次新價格並更新網站。

---

## 三、想在自己電腦上打開（可選）

如果你想先在自己電腦上看看，不用碰 GitHub：

1. 打開 https://github.com/b9907051/test
2. 點綠色的 **Code** 按鈕 → **Download ZIP**
3. 下載後解壓縮
4. 進到解壓縮出來的資料夾，找到：
   - **Mac**：雙擊 `啟動網頁_Mac.command`
   - **Windows**：雙擊 `啟動網頁_Windows.bat`
5. 瀏覽器會自動打開儀表板

要關掉的話，直接關掉跳出來的那個黑色視窗就好。

### 如果雙擊沒反應

代表你的電腦沒有 Python。到 https://www.python.org/downloads/ 下載安裝
（Windows 安裝時**務必勾選 Add Python to PATH**），然後再雙擊一次。

### Mac 顯示「無法打開，因為來自未識別的開發者」

在該檔案上**按右鍵** → 選 **打開** → 再按一次 **打開**。只需要做這一次。

---

## 四、看懂這個儀表板

從上到下：

### 最上面一排控制項
- **計價類型**：「隨需」是隨時可用、貴一點；「競價」便宜但可能被中斷
- **區間**：看最近 30 天還是一整年
- **幣別**：可以切成台幣，匯率可以自己改
- **比較加速器**：點晶片名稱來新增或移除，最多同時比 6 款

### 四張數字卡
- **AI 算力價格指數**：整體行情。以第一天為 100，現在 82 就代表整體便宜了 18%
- **H100 中位數價格**：最主流的晶片，各家報價的中間值
- **H100 今日最低價**：現在誰最便宜
- **30 日變動最大**：哪款晶片這個月動最多

> 綠色 ▼ 代表變便宜（對租的人是好事），紅色 ▲ 代表變貴。

### 價格走勢圖
滑鼠移上去會出現十字線和當天各款價格。點下面的圖例可以暫時隱藏某條線。

### 供應商比較
選一款晶片，看各家報價由便宜排到貴。**藍色那條是最便宜的。**

### 性價比
不是看誰最便宜，而是看「**每單位算力**多少錢」。
有些卡貴但很快，換算下來反而划算。

### 市場基準指數比較
把本站的「牌價」和業界指數（Ornn、Computable 等）疊在一起。
最右邊那欄「**本站 vs 指數**」如果是 +30%，代表牌價比市場實際成交價高三成，
**這大致就是你可以殺價的空間**。

### Token 經濟
GPU 是「進貨成本」，token 是「賣出價格」。這區塊追蹤：
- **各模型每百萬 token 的牌價**（階梯圖，每一階就是一次調價）
- **OpenRouter 上哪些模型最多人用**、平台每週總量成長多少
- **各公司公開講過的 token 處理總量**（Google 每月 3.2 千兆等）
- 一個橋接數字：**租一小時 H100 的錢可以買多少 Sonnet 5 token**

想看到真實的用量資料，需要一把免費的 OpenRouter 金鑰（見第五節）。

### 價格矩陣
一眼看完所有晶片 × 所有供應商。顏色越深代表在同一款晶片裡越貴，
**粗黑框是該款最便宜的那家**。

### 最新報價明細
完整表格，點欄位標題可以排序。右上角有 **匯出 CSV**，可以丟進 Excel。

---

## 五、關於「示範資料」的黃色提示

網頁上方如果有黃色提示說是**示範資料**，代表目前圖表上的數字是**我編的**，
只是為了讓你先看到長相，**不是真實報價**。

做完上面第二節的步驟 1 到 3 之後，收集程式跑成功一次，
示範資料就會自動被真實價格取代，黃色提示也會消失。

### 有些供應商需要帳號才抓得到

免費、不用帳號的來源（Vast.ai、RunPod、Computable、Ornn）會自動運作。
以下是**可選的**，有就更完整，沒有也不影響：

到 https://github.com/b9907051/test/settings/secrets/actions
點 **New repository secret** 逐一新增：

| 名稱 | 用途 | 去哪拿 |
|---|---|---|
| `LAMBDA_API_KEY` | Lambda 的價格 | Lambda 帳號後台 |
| `RUNPOD_API_KEY` | RunPod 更完整的價格 | RunPod 帳號後台 |
| `ORNN_API_KEY` | Ornn 指數完整歷史 | data.ornn.com |
| `OPENROUTER_API_KEY` | Token 用量（Top 模型、每週總量） | openrouter.ai → Settings → Keys，註冊免費、不需付款 |

### 公有雲（AWS、Google、Azure）的價格

這幾家沒有免費的查價 API，需要**人工填**。
編輯 `data/list_prices.json`，把 `usd` 填上價格、`verified_on` 填上你查證的日期：

```
{"provider": "aws", "gpu": "h100-sxm", "type": "on-demand",
 "usd": 6.88, "instance": "p5.48xlarge (8x H100) / 8",
 "region": "us-east-1", "verified_on": "2026-09-16",
 "source_url": "https://aws.amazon.com/ec2/pricing/on-demand/"}
```

> 注意：價格要填「**一顆**晶片一小時多少錢」。
> AWS 的 p5.48xlarge 是 8 顆一台，所以要把整台的價格除以 8。

不想自己填也沒關係，那幾家就會是空白，其他家照常運作。

---

## 六、出問題時怎麼辦

| 症狀 | 怎麼辦 |
|---|---|
| 網址打開是 404 | 等 2 分鐘重整。還是不行就回到步驟 2 確認 Source 選了 GitHub Actions |
| Actions 出現**紅色叉叉** | 點進去看哪一步紅色。多半是步驟 1 的權限沒設好 |
| 網頁一片空白 | 你可能是直接雙擊 `index.html`。這樣不行，要用第三節的啟動檔 |
| 價格好幾天沒更新 | 到 Actions 看最近一次是不是失敗了 |
| 黃色「示範資料」一直在 | 收集程式還沒成功跑過，看 Actions 的紀錄 |

### 怎麼看 Actions 的錯誤訊息

1. 打開 https://github.com/b9907051/test/actions
2. 點最上面那一列
3. 點左邊的 **collect**
4. 展開紅色那一步，裡面的英文訊息就是原因

看不懂的話，把那段訊息複製給我，我幫你看。

---

## 七、想改東西的時候

這些都不用寫程式，改文字檔就好：

| 想做什麼 | 改哪裡 |
|---|---|
| 加一款新晶片 | `data/catalog.json` |
| 加一家新供應商 | `data/catalog.json` |
| 填公有雲價格 | `data/list_prices.json` |
| 模型官方調價 | `data/token_manual.json` 的 `prices` |
| 加一個新模型 | `data/token_catalog.json` |
| 改更新時間 | `.github/workflows/update-prices.yml` 裡的 `cron` |
| 改台幣預設匯率 | 網頁上直接改就好，會記住 |

比較複雜的（換配色、加新圖表、接新的資料來源）再找我，
直接說「我想要⋯⋯」就可以，不用管技術細節。

---

## 附錄：檔案是做什麼的

```
index.html                 網頁本體
assets/                    外觀和圖表程式
data/catalog.json          晶片和供應商清單      ← 可以改
data/prices.json           價格歷史（程式自動寫） ← 不要手動改
data/list_prices.json      公有雲價格            ← 可以改
data/indices.json          市場指數（程式自動寫） ← 不要手動改
scripts/                   抓價格的程式
.github/workflows/         每天自動執行的設定
啟動網頁_Mac.command        Mac 雙擊啟動
啟動網頁_Windows.bat        Windows 雙擊啟動
```
